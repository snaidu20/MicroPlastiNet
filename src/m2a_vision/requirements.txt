# M2a Vision DL — MicroPlastiNet
# Python 3.11 | Module 2a: Vision Deep Learning Pipeline
# ============================================================
# Pin to tested versions for reproducibility.
# GPU-enabled PyTorch: replace torch/torchvision with CUDA builds:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Core DL ──────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Image Processing ─────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.8.0

# ── Numerics ─────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── Training Utilities ───────────────────────────────────────
tensorboard>=2.14.0

# ── Evaluation & Metrics ─────────────────────────────────────
scikit-learn>=1.3.0
matplotlib>=3.7.0

# ── Optional: Production YOLOv8 (preferred over TinyYOLO) ────
# pip install ultralytics>=8.0.0
# ultralytics>=8.0.0

# ── Optional: COCO mAP evaluation ────────────────────────────
# pip install pycocotools
# pycocotools>=2.0.7

# ── Optional: Experiment tracking ────────────────────────────
# wandb>=0.16.0

# ── Data ─────────────────────────────────────────────────────
# Real dataset download (when available):
#   kaggle datasets download mathieuduverne/microplastic-detection
#   kaggle datasets download sanghyeonaustinpark/mpset
# kaggle>=1.5.16
